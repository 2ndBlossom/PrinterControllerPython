from PMoveXYZ import PMoveX, PMoveY, PMoveZ

def custom(x, y, z, vx, vy, vz):
    ## pattern 1
    # PMoveY(y, vy, 1.5);
    # PMoveX(x, vx, 0.5);
    # PMoveY(y, vy, -1.5);
    # PMoveX(x, vx, 1);
    # PMoveY(y, vy, 2.5);
    # PMoveX(x, vx, 0.5);
    # PMoveY(y, vy, -2.5);
    # PMoveX(x, vx, 1);
    # PMoveY(y, vy, 4);
    # PMoveX(x, vx, 0.5);
    # PMoveY(y, vy, -4);
    # 
    # PMoveZ(z, vz, 1);
    # PMoveX(x, vx, -3.5);
    # PMoveZ(z, vz, -1);
    # 
    # PMoveY(y, vy, 1.5);
    # PMoveX(x, vx, 0.5);
    # PMoveY(y, vy, -1.5);
    # PMoveX(x, vx, 1);
    # PMoveY(y, vy, 2.5);
    # PMoveX(x, vx, 0.5);
    # PMoveY(y, vy, -2.5);
    # PMoveX(x, vx, 1);
    # PMoveY(y, vy, 4);
    # PMoveX(x, vx, 0.5);
    # PMoveY(y, vy, -4);

    ## pattern 2
    # PMoveY(y, vy, 3);
    # PMoveX(x, vx, 0.1);
    # PMoveY(y, vy, -1);
    # PMoveX(x, vx, 0.2);
    # PMoveY(y, vy, 1);
    # PMoveX(x, vx, 0.3);
    # PMoveY(y, vy, -1);
    # PMoveX(x, vx, 0.4);
    # PMoveY(y, vy, 1);
    # PMoveX(x, vx, 0.5);
    # PMoveY(y, vy, -1);
    # PMoveX(x, vx, 0.4);
    # PMoveY(y, vy, 1);
    # PMoveX(x, vx, 0.3);
    # PMoveY(y, vy, -1);
    # PMoveX(x, vx, 0.2);
    # PMoveY(y, vy, 1);
    # PMoveX(x, vx, 0.1);
    # PMoveY(y, vy, -3);
    # 
    # PMoveZ(z, vz, 1);
    # PMoveX(x, vx, -2.5);
    # PMoveZ(z, vz, -1);
    # 
    # PMoveY(y, vy, 3);
    # PMoveX(x, vx, 0.1);
    # PMoveY(y, vy, -1);
    # PMoveX(x, vx, 0.2);
    # PMoveY(y, vy, 1);
    # PMoveX(x, vx, 0.3);
    # PMoveY(y, vy, -1);
    # PMoveX(x, vx, 0.4);
    # PMoveY(y, vy, 1);
    # PMoveX(x, vx, 0.5);
    # PMoveY(y, vy, -1);
    # PMoveX(x, vx, 0.4);
    # PMoveY(y, vy, 1);
    # PMoveX(x, vx, 0.3);
    # PMoveY(y, vy, -1);
    # PMoveX(x, vx, 0.2);
    # PMoveY(y, vy, 1);
    # PMoveX(x, vx, 0.1);
    # PMoveY(y, vy, -3);

    ## pattern 3

    PMoveY(y, vy, 5)#zpy: wthy set so many times?
    PMoveX(x, vx, 5)
    PMoveY(y, vy, -5)
    PMoveX(x, vx, -4.7)
    PMoveY(y, vy, 4.7)
    PMoveX(x, vx, 4.4)
    PMoveY(y, vy, -4.4)
    PMoveX(x, vx, -4.1)
    PMoveY(y, vy, 4.1)
    PMoveX(x, vx, 3.8)
    PMoveY(y, vy, -3.8)
    PMoveX(x, vx, -3.5)
    PMoveY(y, vy, 3.5)
    PMoveX(x, vx, 3.2)
    PMoveY(y, vy, -3.2)
    PMoveX(x, vx, -2.9)
    PMoveY(y, vy, 2.9)
    PMoveX(x, vx, 2.6)
    PMoveY(y, vy, -2.6)
    PMoveX(x, vx, -2.3)

    PMoveZ(z, vz, 1)
    PMoveX(x, vx, -1.5)
    PMoveY(y, vy, -1.2)
    PMoveZ(z, vz, -1)

    # PMoveY(y, vy, 5);
    # PMoveX(x, vx, 5);
    # PMoveY(y, vy, -5);
    # PMoveX(x, vx, -4.7);
    # PMoveY(y, vy, 4.7);
    # PMoveX(x, vx, 4.4);
    # PMoveY(y, vy, -4.4);
    # PMoveX(x, vx, -4.1);
    # PMoveY(y, vy, 4.1);
    # PMoveX(x, vx, 3.8);
    # PMoveY(y, vy, -3.8);
    # PMoveX(x, vx, -3.5);
    # PMoveY(y, vy, 3.5);
    # PMoveX(x, vx, 3.2);
    # PMoveY(y, vy, -3.2);
    # PMoveX(x, vx, -2.9);
    # PMoveY(y, vy, 2.9);
    # PMoveX(x, vx, 2.6);
    # PMoveY(y, vy, -2.6);
    # PMoveX(x, vx, -2.3);
    # 
    # PMoveZ(z, vz, 1);
    # PMoveX(x, vx, -1.5);
    # PMoveY(y, vy, -1.2);
    # PMoveZ(z, vz, -1);